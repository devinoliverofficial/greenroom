/* Filled in once the Supabase project exists. Both values are public by
   design (the anon key is meant to ship in the page; row-level security is
   what protects the data). Empty strings = the app runs on-device only. */
window.GREENROOM_CONFIG = {
  url: '',
  anonKey: ''
};
